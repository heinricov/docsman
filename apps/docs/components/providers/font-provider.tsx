import { Geist_Mono, Oxanium } from "next/font/google"

export const oxanium = Oxanium({ subsets: ["latin"], variable: "--font-sans" })

export const fontMono = Geist_Mono({
  subsets: ["latin"],
  variable: "--font-mono",
})
