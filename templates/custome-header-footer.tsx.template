import "./globals.css"
import { ThemeProvider } from "@/components/theme-provider"
import { DocsmanLayout } from "docsman/layouts"
import { MyHeader } from "@/components/MyHeader"
import { MyFooter } from "@/components/MyHeader"

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body>
        <ThemeProvider>
          <DocsmanLayout
          //  ini untuk menggunakan Header dan Footer Custome miliki sendiri
            Header={<MyHeader />}
            Footer={<MyFooter/>}
          >
            {children}
          </DocsmanLayout>
        </ThemeProvider>
      </body>
    </html>
  )
}
